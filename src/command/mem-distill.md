---
description: 工作流提炼（distill）：识别近一月可复用的人工工作流，沉淀为 skill/agent/命令
---

执行一次 distill 工作流提炼：回顾近一个月会话，识别值得打包的可复用人工工作流。先盘点现有 skills/agents/commands 避免重复，产出精简清单，只创建高置信缺失资产。间隔检查由插件自动处理（默认 30 天一次，不足则插件已拦截本次执行）。
